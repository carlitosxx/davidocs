import 'package:flutter/material.dart';

class PendingDocumentPhoneView extends StatelessWidget {
  const PendingDocumentPhoneView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vista previa'),
      ),
    );
  }
}
