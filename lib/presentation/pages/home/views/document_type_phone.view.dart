import 'package:flutter/material.dart';

class DocumentTypePhoneView extends StatelessWidget {
  const DocumentTypePhoneView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
